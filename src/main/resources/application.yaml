spring:
  application:
    name: order-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:9011}"
  cloud:
    config:
      discovery:
        enabled: false
      profile: ${SPRING_PROFILES_ACTIVE:local}