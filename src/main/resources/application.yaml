spring:
  application:
    name: order-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
    group:
      local: "local,topics"
      dev: "dev,topics"
      prod: "prod,kafka,topics"
  config:
    import:
      - "optional:configserver:"
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
slack:
  token: ${SLACK_TOKEN}
  channel: ${SLACK_CHANNEL}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:9001/eureka/}